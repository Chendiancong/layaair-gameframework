import { UIPanel } from "../core/UIView";
import { IViewLayerConfig, ViewLayerMgr } from "../core/ViewLayerMgr";
import { ViewMgr } from "../core/ViewMgr";

export class UITest extends UIPanel {
    onOpen(time?: number): void {
    }
}